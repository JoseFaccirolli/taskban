Rode no terminal {
nano ~/.bashrc
}

Vá até o final do arquivo e cole {
# Meus Atalhos Personalizados
alias atualizar='sudo apt update && sudo apt full-upgrade -y && sudo apt autoremove -y'
alias instalar='sudo apt install'
alias som='alsamixer'

# Navegação e Arquivos
alias listar='ls -la --color=auto'
alias voltar='cd ..'
alias apagar='rm -i'

# Sistema
alias desligar='sudo shutdown -h now'
alias reiniciar='sudo reboot'
alias memoria='free -h'

# Manutenção Extra
alias limpar='sudo apt autoremove -y && sudo apt clean'

# Rede (bônus: mostra seu IP de internet)
alias meuip='curl ifconfig.me'
}